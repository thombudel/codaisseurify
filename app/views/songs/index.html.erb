<h1>Songs</h1>
<div class="song">
  <% @songs.each do |song| %>
    <p><%= link_to song.name, song_path(song.id) %></p>
    <p><%= song.duration %></p>
    <p><%= song.genre %></p>
  <% end %>
</div>

<hr>
<%= link_to "[Add New Song]", new_song_path %>
