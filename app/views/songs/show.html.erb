<p id="notice"><%= notice %></p>

<h1><%= @song.name %></h1>

<%= link_to "[All Songs]", songs_path %>

<%= link_to "[Delete Song]", song_path(@song.id), method: :delete, data: { confirm: "Are you sure?" } %>
